{
  "name": "devio-validator",
  "version": "0.0.16",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "version-patch": "npm version patch",
    "prepublishOnly": "npm run build",
    "publish-auto": "npm run version-patch && npm publish"
  },
  "peerDependencies": {
    "date-fns": "*",
    "express": "*",
    "zod": "*"
  },
  "keywords": [],
  "author": "Igor Rezende",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^4.17.0",
    "date-fns": "^2.0.0",
    "express": "^4.18.0",
    "zod": "^3.0.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}
